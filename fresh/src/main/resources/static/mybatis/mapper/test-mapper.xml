<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test">
	
	<select id="test" resultType="UserDTO">
		select * from user
	</select>

		<insert id="join" parameterType="UserDTO">
		insert into USER(user_id, user_pw, user_username)
		values (#{user_id}, #{user_pw}, #{user_username})
	</insert>
	
	<select id="getUserData" parameterType="UserDTO" resultType="UserDTO">
		select user_no, user_id, user_pw, user_name, user_email, user_username, user_date, ROLE 
		from user
		where user_id = #{user_id} and ROLE = #{role}
	</select>
</mapper>